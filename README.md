# Wealth Action Playbook (React + Vite)

## Run locally
```bash
npm i
npm run dev
```

## Build
```bash
npm run build
```

The app stores user auth and progress in `localStorage` via a simple async wrapper exposed as `window.storage`.
